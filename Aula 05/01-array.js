// Declaração
const numeros = []; 

// Atribuição
numeros[0] = 1;
numeros[1] = 2;
numeros[2] = 3;

// Acesso
console.log(numeros);
console.log(numeros[0]);

// Inicilização
const notas = [7.6, 4.5, 8.7];
console.log(notas);