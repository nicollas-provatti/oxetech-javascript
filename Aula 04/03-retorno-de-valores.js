// Retorno: resultado retornado por uma função
function somar(a, b) {
  return a + b; // retorno
}

let resultado = somar(2, 3);
console.log(resultado);

// Se a função não usar return, ela retorna undefined por padrão.